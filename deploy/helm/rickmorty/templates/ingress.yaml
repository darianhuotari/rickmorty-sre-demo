{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "rickmorty.fullname" . }}
  labels:
    {{- include "rickmorty.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.ingress.rateLimit }}
    nginx.ingress.kubernetes.io/limit-rps: "{{ .rps }}"
    nginx.ingress.kubernetes.io/limit-burst: "{{ .burst }}"
    nginx.ingress.kubernetes.io/limit-no-delay: "{{ .noDelay }}"
    nginx.ingress.kubernetes.io/limit-connections: "{{ .connections }}"
    nginx.ingress.kubernetes.io/limit-req-status-code: "{{ .statusCode }}"
    nginx.ingress.kubernetes.io/limit-req-zone-variable: "{{ .key }}"
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path | quote }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "rickmorty.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
{{- end }}
